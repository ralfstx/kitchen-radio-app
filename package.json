{
  "name": "kitchen-radio-app",
  "version": "0.0.1",
  "main": "build/src/main.js",
  "scripts": {
    "lint": "jshint",
    "build": "babel --copy-files --out-dir build/src/ src/",
    "watch": "babel --watch --copy-files --out-dir build/src/ src/",
    "pretest": "babel --copy-files --out-dir build/spec/ spec/",
    "test": "jasmine"
  },
  "dependencies": {
    "tabris": "https://tabrisjs.com/downloads/nightly/tabris.tgz",
    "promise": "^6.1.0",
    "whatwg-fetch": "^0.7.0",
    "underscore": "^1.7.0"
  },
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-polyfill": "^6.5.0",
    "babel-preset-es2015": "^6.5.0",
    "grunt": "^0.4.5",
    "grunt-contrib-clean": "^1.0.0",
    "grunt-contrib-copy": "^0.8.0",
    "grunt-shell": "^1.1.2"
  }
}
