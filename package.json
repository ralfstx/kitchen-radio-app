{
  "name": "kitchen-radio-app",
  "version": "0.0.1",
  "main": "build/main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/ralfstx/kitchen-radio-app.git"
  },
  "license": "MIT",
  "scripts": {
    "build:ios": "BABEL_ENV=ios npm run dist",
    "build:android": "BABEL_ENV=android npm run dist",
    "dist": "rm -rf build/ && babel --copy-files --out-dir build/ src/",
    "mocha": "mocha --compilers js:babel-core/register",
    "lint": "eslint .",
    "test": "npm run mocha -- 'test/**/*.spec.js'",
    "posttest": "npm run lint",
    "server": "hs -c-1",
    "start": "npm run dist && npm run server"
  },
  "dependencies": {
    "core-js": "^2.2.1",
    "tabris": "^1.10.0",
    "underscore": "^1.7.0"
  },
  "devDependencies": {
    "babel-cli": "^6.14.0",
    "babel-core": "^6.14.0",
    "babel-preset-es2015": "^6.14.0",
    "chai": "^3.5.0",
    "eslint": "^3.4.0",
    "fetch-mock": "^5.1.1",
    "mocha": "^3.2.0",
    "sinon": "^1.17.3",
    "sinon-chai": "^2.8.0"
  }
}
