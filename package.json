{
  "name": "kitchen-radio-app",
  "version": "0.0.1",
  "main": "dist/main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/ralfstx/kitchen-radio-app.git"
  },
  "license": "MIT",
  "scripts": {
    "build:ios": "BABEL_ENV=ios npm run dist",
    "build:android": "BABEL_ENV=android npm run dist",
    "dist": "rm -rf dist/ && mkdir dist/ && cp -a images dist && npm run rollup",
    "rollup": "rollup --format=cjs --output=dist/main.js --external tabris,underscore,core-js/client/shim.min.js -- src/main.js",
    "mocha": "mocha --compilers js:babel-core/register",
    "lint": "eslint .",
    "test": "npm run mocha -- 'test/**/*.spec.js'",
    "posttest": "npm run lint",
    "server": "hs -c-1",
    "start": "npm run dist && npm run server"
  },
  "dependencies": {
    "core-js": "^2.2.1",
    "tabris": "2.0.0-beta2",
    "underscore": "^1.7.0"
  },
  "devDependencies": {
    "babel-core": "^6.14.0",
    "babel-plugin-transform-es2015-modules-commonjs": "^6.23.0",
    "chai": "^3.5.0",
    "eslint": "^3.4.0",
    "fetch-mock": "^5.1.1",
    "mocha": "^3.2.0",
    "rollup": "^0.41.4",
    "sinon": "^1.17.3",
    "sinon-chai": "^2.8.0"
  }
}
